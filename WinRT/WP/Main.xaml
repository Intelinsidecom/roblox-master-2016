<Page
    x:Class="WP.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid>
    <!-- Main rendering surface: engine presents its swap chain here. -->
    <SwapChainPanel x:Name="swapChainPanel" />

    <Grid x:Name="OverlayRoot" Visibility="Collapsed" Background="#80000000">
      <Grid x:Name="LoginPanel" Visibility="Collapsed">
        <TextBlock Text="Login (placeholder)"
                   Foreground="White" FontSize="20"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
      </Grid>
      <Grid x:Name="MenuPanel" Visibility="Collapsed">
        <TextBlock Text="Menu (placeholder)"
                   Foreground="White" FontSize="20"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
      </Grid>
      <Grid x:Name="HomePanel" Visibility="Collapsed">
        <TextBlock Text="Home (placeholder)"
                   Foreground="White" FontSize="20"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
      </Grid>
    </Grid>
  </Grid>
</Page>
